# ==========================================
# MyGameList - Variables d'Environnement
# ==========================================

# ==========================================
# BACKEND
# ==========================================

# Environment
NODE_ENV=development

# Server
PORT=3001

# Database (PostgreSQL)
DB_HOST=postgres
DB_PORT=5432
DB_NAME=mygamelist_dev
DB_USER=dev_user
DB_PASSWORD=dev_password
DB_POOL_MIN=2
DB_POOL_MAX=10

# Redis Cache
REDIS_HOST=redis
REDIS_PORT=6379
# REDIS_PASSWORD=  # Laisser vide en développement

# Session
SESSION_SECRET=dev_secret_change_in_production_minimum_32_chars
SESSION_MAX_AGE=86400000  # 24 heures en millisecondes
SESSION_NAME=sessionId

# Security
BCRYPT_ROUNDS=10
RATE_LIMIT_WINDOW_MS=900000  # 15 minutes
RATE_LIMIT_MAX_REQUESTS=100

# CORS
CORS_ORIGIN=http://localhost:5173

# Logs
LOG_LEVEL=debug

# ==========================================
# FRONTEND
# ==========================================

# API URL
VITE_API_URL=http://localhost:3001

# Environment
VITE_NODE_ENV=development

# Hot Module Replacement
VITE_HMR_HOST=localhost
VITE_PORT=5173

# ==========================================
# DOCKER (docker-compose.dev.yml)
# ==========================================

# Ces variables sont déjà définies dans docker-compose.dev.yml
# Décommenter si besoin de les surcharger

# POSTGRES_DB=mygamelist_dev
# POSTGRES_USER=dev_user
# POSTGRES_PASSWORD=dev_password

# ==========================================
# PRODUCTION (à configurer séparément)
# ==========================================

# NODE_ENV=production
# DB_PASSWORD=<strong_password>
# REDIS_PASSWORD=<strong_password>
# SESSION_SECRET=<strong_secret_minimum_32_chars>
# CORS_ORIGIN=https://mygamelist.com
# VITE_API_URL=https://api.mygamelist.com

# ==========================================
# NOTES
# ==========================================

# 1. Copier ce fichier en .env pour le backend
# 2. NE JAMAIS commit le fichier .env
# 3. En production, utiliser des secrets forts
# 4. SESSION_SECRET doit faire au moins 32 caractères
# 5. Changer TOUS les mots de passe en production
